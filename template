question

1) need new table
column = user id | user name | Question | answer (later)
if user is logged in > then can ask question
else
they will see " please login / signup to ask question"
How to display all question in you home feeds?
In user profile page. Only display question ask by this user.


2) Old table
add username in signup form
signin still via user email
